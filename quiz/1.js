// ร้านค้าขายมือถือแห่งหนึ่ง จะขายโทรศัพท์ให้กับผู้ซื้อที่อายุ 20 ปีเท่านั้น
export default function quiz(input) {
  let answer = false;
  const buyerAge = input;
  if (0) {
    answer = true;
  }
  return answer;
}
